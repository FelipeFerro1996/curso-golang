info:
  name: INDEX
  type: http
  seq: 2

http:
  method: GET
  url: localhost:5000/usuarios
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
